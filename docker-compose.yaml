version: "3.9"

services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
     - "8000:8000"
# expose the port 8000 to the host machine     
    volumes:
      - ./app:/app
    command: > 
      sh -c "python manage.py runserver 0.0.0.0:8000"